# Core Python dependencies for the CODA-LM + LLaVA-1.5 release.
#
# Install torch/torchvision separately so the CUDA wheel matches your machine.
# See INSTALL.md for a copy-paste setup.

accelerate>=0.29.0
einops==0.6.1
einops-exts==0.0.4
numpy>=1.24.0,<2.0
pillow>=10.0.0
pyyaml>=6.0.1
requests>=2.31.0
safetensors>=0.4.0
sentencepiece==0.1.99
shortuuid>=1.0.11
timm>=1.0.0
tokenizers>=0.19,<0.20
tqdm>=4.66.0
transformers==4.40.0
